services:
  union-svc:
    image: ghcr.io/lylighte/elementskin-union-svc:latest
    restart: always
    ports:
      - "8267:8267"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
